name: '${project.name}'
version: '${project.version} for Minecraft ${plugin.minecraft} - ${describe}'
authors:
- TheMolkaPL
website: http://www.shootgame.net
description: '${project.description}'
prefix: '${project.name}'
load: STARTUP
loadbefore:
- WorldEdit
dependency:
- Permissions
- ShootGame
main: '${plugin.bukkit.mainClass}'

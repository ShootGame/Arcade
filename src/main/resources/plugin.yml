name: '${project.name}'
version: 'Minecraft ${plugin.minecraft}: ${project.version}-${git.commit.id.abbrev}'
authors:
- TheMolkaPL
website: http://www.shootgame.net
description: '${project.description}'
prefix: '${project.name}'
load: STARTUP
loadbefore:
- WorldEdit
dependency:
- Permissions
- ShootGame
main: '${plugin.bukkit.mainClass}'
